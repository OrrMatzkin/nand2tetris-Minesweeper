/** Initializes a minesweeper game and starts running it. */
class Main {
    function void main() {
        var MinesweeperGame game;
        let game = MinesweeperGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}